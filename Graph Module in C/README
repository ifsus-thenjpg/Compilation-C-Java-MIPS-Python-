Brief description of submitted files:

findComponents.c
- A client that will find all the connected components of Graph G by running DFS(G) twice using Graph ADT dependent on List ADT

Graph.c
- ADT class that manages an array of Lists representing an adjacency matrix for graph G, implements BFS

List.c
- ADT class that manages a doubly linked list with a cursor that is used to insert an element before/after to help in
indirectly sorting a list of strings by using the indices of the list.

List.h
- header files

Graph.h
- header files

GraphTest.c
- A client to help test Graph.java.

README
- description of submitted files